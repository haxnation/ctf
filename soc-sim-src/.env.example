# Base URL of the SOC Simulation API (no trailing slash)
VITE_API_BASE_URL=http://localhost:3000

# JWT secret used for simulated login token generation
# Must match JWT_SECRET in the backend .env
VITE_JWT_SECRET=haxnation-dev-secret
